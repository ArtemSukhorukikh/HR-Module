version: '3'
services:
    web:
        build: .
        stdin_open: true
        tty: true
        ports: 
            - "8086:8086"
        volumes:
            - "/app/node_modules" 
            - ".:/app"
